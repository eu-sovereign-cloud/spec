# GENERATED - DO NOT EDIT THIS FILE DIRECTLY
openapi: 3.0.3

servers:
  - url: https://demo.secapi.cloud/providers/seca.activity-log
    description: Path Schema
  - url: https://activity-log.seca.demo.secapi.cloud
    description: DNS Schema

info:
  title: Activity Log
  version: v1
  description: |
    The Activity Log API provides access to audit logs for all resources.
    This API is part of the Sovereign European Cloud API.

security:
  - bearerAuth: []

tags:
  - name: Activity Log
    description: Activity Log management

paths:
  /v1/tenants/{tenant}/workspaces/{workspace}/activity-logs:
    get:
      tags:
        - Activity Log
      security:
        - bearerAuth: []
      summary: List activity logs
      description: |
        Lists activity logs and provides paginated, filtered access.
      operationId: listActivityLogs
      parameters:
        - $ref: './schemas/parameters.yaml#/parameters/tenant'
        - $ref: './schemas/parameters.yaml#/parameters/workspace'
        - $ref: './schemas/parameters.yaml#/parameters/labelSelector'
        - $ref: './schemas/parameters.yaml#/parameters/limitParam'
        - $ref: './schemas/parameters.yaml#/parameters/skipTokenParam'
        - $ref: './schemas/parameters.yaml#/parameters/acceptHeader'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActivityLogIterator"
        '400':
          $ref: './schemas/errors.yaml#/responses/Error400'
        '401':
          $ref: './schemas/errors.yaml#/responses/Error401'
        '403':
          $ref: './schemas/errors.yaml#/responses/Error403'
        '500':
          $ref: './schemas/errors.yaml#/responses/Error500'
  
components:
  securitySchemes:
    $ref: './schemas/security.yaml#/securitySchemes'

  schemas:
    ActivityLogIterator:
      description: Iterator for activity logs
      type: object
      required:
        - items
        - metadata
      properties:
        items:
          description: List of activity logs
          type: array
          items:
            $ref: './schemas/activity-log.yaml#/ActivityLog'
        metadata:
          $ref: './schemas/resource.yaml#/ResponseMetadata'
